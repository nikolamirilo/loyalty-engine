[pytest]
# Only the pytest suite. The scripts directly under tests/ are standalone
# regression programs with their own entry points, not pytest tests, and one of
# them fails to import by design (see README, Testing).
testpaths = tests/integration
addopts = -v --tb=short
