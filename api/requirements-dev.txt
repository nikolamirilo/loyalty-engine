# Test-only dependencies. Install alongside requirements.txt:
#   pip install -r requirements.txt -r requirements-dev.txt
pytest==8.3.3
# Backs fastapi.testclient.TestClient, which calls the app in process.
httpx==0.27.2
