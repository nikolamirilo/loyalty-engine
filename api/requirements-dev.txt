# Test-only dependencies. Install alongside requirements.txt:
#   pip install -r requirements.txt -r requirements-dev.txt
pytest==8.3.3
# fastapi.testclient.TestClient also needs httpx, which requirements.txt
# already pins (the logo upload uses it at runtime).
