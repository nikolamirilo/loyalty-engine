**Important Rules**ß:
- API should always return fields in camelCase format
- Client app should access data only using API server, it should not access DB directly
- UI/UX should be as simple as possible, so everyone can intutively use it
- use S.O.L.I.D. principles for architecturing app
